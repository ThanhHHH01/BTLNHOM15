@model BTLNHOM15.Models.ChucVu

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <p>
    <a asp-action="Create" class="btn btn-success">Chức vụ</a>
</p>

    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TenChucVu)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TenChucVu)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.MaChucVu" class="btn btn-primary">Chỉnh sửa</a> 
    <a asp-action="Index" class="btn btn-primary">Trở về</a>
</div>
