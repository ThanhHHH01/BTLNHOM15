@model BTLNHOM15.Models.GoiTap

@{
    ViewData["Title"] = "Details";
}

<h1>Thông tin</h1>

<div>
    <p>
    <a asp-action="Create" class="btn btn-success">Gói tập</a>
</p>

    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.TenGoi)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.TenGoi)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.GiaGoi)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.GiaGoi)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model?.MaGoiTap" class="btn btn-primary">Chỉnh sửa</a> 
    <a asp-action="Index" class="btn btn-primary">Trở về</a>
</div>
