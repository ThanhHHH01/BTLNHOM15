@model IEnumerable<BTLNHOM15.Models.GoiTap>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.GiaGoi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.GiaGoi.MaGiaGoi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.GoiID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.GoiID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.GoiID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
