@model IEnumerable<BTLNHOM15.Models.GiaGoi>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenGoi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenGoi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MaGiaGoi">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MaGiaGoi">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MaGiaGoi">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
